# Ardur Phase 2 eBPF MVP Tool Inventory — 2026-05-10

Status: pre-implementation inventory and readiness gate.
Generated by: Hermes local operator.
Public/GitHub action: none.

## Objective

Start Phase 2 with a narrow Linux eBPF MVP: prove Ardur can load Linux eBPF process-exec and paired process-exit producers in a local Linux test environment, read their scoped ringbuf metadata events through the Go kernelcapture path, and render honest synthetic kernel-effect evidence. This is not a production daemon or broad side-effect-capture claim.

## Required tools and current status

| Tool / surface | Required for | Status | Evidence | Action before implementation |
|---|---|---:|---|---|
| macOS host | Local orchestration only | present | Darwin 25.4.0 arm64 | Use only as controller; do not claim native macOS eBPF. |
| Homebrew | Optional host package manager | present | Homebrew 5.1.10 | No host package install required for first MVP. |
| Go | Ardur Go package/tests | present | go1.25.9 darwin/arm64 | Use host Go for non-privileged package tests and cross-compile checks. |
| Python | Repo scripts/checks | present | Python 3.9.6 plus repo bootstrap Python 3.13.13 observed earlier | Use existing repo scripts only. |
| Podman client | Docker-compatible local container runtime | present | Podman 5.8.2 client | Use existing Podman; Docker CLI is symlinked to Podman. |
| Podman Linux VM | Linux kernel target for eBPF testing | running | podman-machine-default, applehv, 2 CPU, 2 GiB RAM, 100 GiB disk | Use this VM for Linux-only/gated tests. |
| Linux kernel | eBPF runtime target | present | Fedora CoreOS kernel 6.19.7-200.fc43.aarch64 | Target linux/arm64 first. |
| BTF | CO-RE/eBPF metadata | present | /sys/kernel/btf/vmlinux readable | Required eBPF preflight passes. |
| bpffs | BPF filesystem/map pinning | mounted | /sys/fs/bpf bpf rw,nosuid,nodev,noexec,relatime,mode=700 | Use only daemon/test-owned paths; no repo-controlled privileged path. |
| Unprivileged BPF | Determines need for privileged test | disabled | /proc/sys/kernel/unprivileged_bpf_disabled = 2 | Gated smoke must run in privileged local Linux container/VM. |
| Privileged Linux container | Narrow eBPF smoke execution | available | Fedora 43 privileged container CapEff=000001ffffffffff | Allowed for local MVP smoke; no service exposure. |
| clang / LLVM | Compile eBPF C program | present in local eBPF dev image | clang 21.1.8 / LLVM 21.1.8 | Built into `ardur-ebpf-dev:fedora43`. |
| bpftool | Inspect BPF/BTF capabilities | present in local eBPF dev image | bpftool v7.6.0, libbpf v1.6 | Built into `ardur-ebpf-dev:fedora43`. |
| libbpf headers | eBPF helper headers | present in local eBPF dev image | libbpf-devel 1.6.1 | Built into `ardur-ebpf-dev:fedora43`. |
| make/git/findutils | Build/test harness support | present in local eBPF dev image | Fedora 43 packages installed | Built into `ardur-ebpf-dev:fedora43`. |

## Local container image to create

Image tag: `ardur-ebpf-dev:fedora43`
Image ID: `3e386621f3b69c23b90569ae6b195a220a387b8a2dc940a73dc6848f185295e2`
Image size: `1713010481` bytes
Build status: built and smoke-verified in privileged Fedora 43 container on 2026-05-10T06:16Z.

Packages planned inside image:

- `golang`
- `clang`
- `llvm`
- `llvm-devel`
- `bpftool`
- `libbpf-devel`
- `kernel-headers`
- `make`
- `git`
- `findutils`
- `which`

This keeps host macOS clean and places Linux/eBPF-specific tooling inside the Podman VM. It does not expose a service, publish an image, push to a registry, or change GitHub state.

## Implementation-start gate

Implementation may start after these are true:

1. A clean Ardur worktree exists from `origin/dev` for Phase 2 MVP work.
2. The local Linux dev image builds successfully.
3. The dev image can see `/sys/kernel/btf/vmlinux` in a privileged run.
4. The MVP plan and claim boundary are written locally.

## Claim boundary

Allowed after a passing MVP smoke:

- “Local Linux eBPF process-exec smoke harness works in a privileged Podman VM/container.”
- “Ardur can read eBPF ringbuf process exec+exit metadata samples and project them into synthetic kernel-effect receipts.”

Not allowed yet:

- “Ardur has production eBPF support.”
- “Ardur captures all subprocess/file/network side effects.”
- “Ardur works tool-agnostically across all CLIs in production.”
- “The privileged daemon is ready for install or broad local enablement.”
